# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'testMusic1' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for testMusic1
  pod 'SDWebImage'
  pod 'SDWebImage/GIF'
  pod 'FLAnimatedImage'
  pod 'SwiftyJSON'

end
